# Use empirical ranges with bootstrap uncertainty and EVT stress estimates

Primary price ranges and breach rates come directly from equal-weight, week-position-matched paths across the full validated history. Contiguous-block bootstrap supplies confidence intervals, while EVT may add a separately labeled extreme-tail stress estimate only when fit diagnostics pass; EVT never assigns Safety Grades or overrides observed extremes, and normal-distribution assumptions are not used.
